export interface UnicornOptions {
  /**
   * Include all rules recommended by `eslint-plugin-unicorn`
   *
   * @default false
   */
  allRecommended?: boolean;
}

export type OptionUnicorn = UnicornOptions
